1) SELECT AVG(sal), MAX(sal), MIN(sal), SUM(sal) FROM emp;

2) SELECT deptno, AVG(sal), MAX(sal), MIN(sal), SUM(sal) FROM emp 
   GROUP BY deptno ORDER BY deptno;

3) SELECT job, AVG(sal), MAX(sal), MIN(sal), SUM(sal) FROM emp 
   GROUP BY job ORDER BY MAX(sal)

6) SELECT job, SUM(sal+NVL(comm,0)) "Total Salary" FROM emp
   GROUP BY job ORDER BY SUM(sal+NVL(comm,0)) DESC;